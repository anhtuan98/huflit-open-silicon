.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# LibreLane / OpenLane run artifacts
runs/
*.gds
*.gdsii
*.lef
*.def.gz

# Simulation / waveform artifacts
*.vcd
*.fst
*.fst.hier
obj_dir/
sim_build/
.pytest_cache/
results.xml

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Docker
*.log

# Scratch / cache / temp files (see CLAUDE.md) — never tracked, not even a placeholder
tmp/

# Claude Code agent worktrees (created for isolated background sub-agent work)
.claude/worktrees/

# Internal working notes (budget drafts, cross-project naming notes, event
# research) — not for this repo's public audience per CLAUDE.md ("Tuyệt đối
# không dùng tool Artifact... brainstorming là bản nháp nội bộ"). Whole
# directory, not just one file -- 2026-09-19: found two more untracked
# files here (budget proposal, ORConf notes) that this narrower rule had
# been about to let slip into a commit.
brainstorming/
